# ===========================================
# SLMS Infrastructure Environment Variables
# ===========================================
# Copy this file to .env and adjust values as needed
# Command: cp env.example .env

# ===========================================
# PostgreSQL Configuration
# ===========================================
POSTGRES_USER=slms
POSTGRES_PASSWORD=slms
POSTGRES_DB=slms
POSTGRES_PORT=5544

# ===========================================
# Redis Configuration
# ===========================================
REDIS_PORT=6379

# ===========================================
# Object Storage (MinIO for local dev)
# ===========================================
# MinIO container settings
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET=slms-docs

# Application storage settings (used by Strapi/API)
# For local dev with MinIO:
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_BUCKET=slms-docs
STORAGE_ACCESS_KEY_ID=minioadmin
STORAGE_ACCESS_KEY_SECRET=minioadmin
STORAGE_REGION=us-east-1
STORAGE_FORCE_PATH_STYLE=true

# For Alibaba Cloud OSS (production):
# STORAGE_ENDPOINT=https://oss-ap-southeast-5.aliyuncs.com
# STORAGE_BUCKET=your-bucket-name
# STORAGE_ACCESS_KEY_ID=your-aliyun-access-key-id
# STORAGE_ACCESS_KEY_SECRET=your-aliyun-access-key-secret
# STORAGE_REGION=oss-ap-southeast-5
# STORAGE_FORCE_PATH_STYLE=false
# STORAGE_BASE_URL=https://your-bucket-name.oss-ap-southeast-5.aliyuncs.com

# ===========================================
# Mailhog Configuration (Email testing)
# ===========================================
MAILHOG_UI_PORT=8025
MAILHOG_SMTP_PORT=1025

# ===========================================
# SLMS API & Web (Docker)
# ===========================================
# Ports when running API/Web in Docker
API_PORT=3001
WEB_PORT=3000

# API URL as seen by the browser (for Next.js build and cookie-based auth)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# In production only: restrict registration to @energi-up.com. Set to "true" in prod; leave unset for dev/local.
# NEXT_PUBLIC_REGISTRATION_DOMAIN_RESTRICTION_ENABLED=false

# ===========================================
# JWT (required for API / admin auth in Docker)
# ===========================================
# Replace with strong secrets in production (e.g. openssl rand -base64 64)
JWT_SECRET=your-jwt-secret-replace-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-secret-replace-in-production
JWT_REFRESH_EXPIRES_IN=7d
JWT_ADMIN_SECRET=your-admin-jwt-secret-replace-in-production
JWT_ADMIN_EXPIRES_IN=15m
